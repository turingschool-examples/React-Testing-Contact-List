// What should we import?


describe('ContactCard', () => {
  it('Should render a card', () => {

  });

  it('Should be able to click the favorite button', () => {

  });

  it('Should be able to click the delete button', () => {

  });

  it('Should render the correct UI if the contact is NOT a favorite', () => {
    
  });

  it('Should render the correct UI if the contact is a favorite', () => {
    
  });
});

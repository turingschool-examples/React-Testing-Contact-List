//What should we import?

describe('Form', () => {
  it('Should render a form', () => {

  });

  it('Should have inputs that hold their value when interacted with', () => {

  });

  it('Should be able to submit the form', () => {

  });

  it('Should clear the inputs on submit', () => {

  });

});
